- def type:
    Box:
    - none: ~ # (~ -> Box) & (Box -> Box)
    - some: Int # (Int -> Box) & (Box -> Int)

- def function: # Box -> Int
    show: Box
  in:
  - some: ~
  - else: 0
  - show: ~

- do:
  - format: "none -> {}, some -> {}"
    with:
      $0: {none}
      $1: {some: 42}
  - show: ~
